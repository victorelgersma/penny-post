<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="stylesheet" href="style.css" class="rel" />
    <meta name="generator" content={Astro.generator} />
    <title>Penny Post</title>
  </head>
  <body>
    <nav>
      <a href="/">Penny Post</a>
      <a href="/login">Login</a>
    </nav>
    <div class="main-wrapper">
      <slot />
    </div>
  </body>
</html>

<style>
  :root {
    --color-nav-banner: var(--color-2);
    --color-banner: var(--color-2);
    --color-form-button: var(--color-2);
    --text-color: var(--color-darkest);
    /* pallette */

    --color-1: #323031;
    --color-2: #3d3b3c;
    --color-3: #7f7979;
    --color-4: #c1bdb3;
    --color-5: #5f5b6b;
  }
  nav {
    background-color: var(--color-nav-banner);
    color: var(--color-4);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1em;
  }
  nav a {
    color: var(--color-4);
    padding: 0.5em;
  }
  a {
    text-decoration: none;
  }
  a:hover {
    color: var(--color-4);
    background-color: var(--color-5);
  }
  h1,
  h3 {
    margin: 0;
    padding: 0.3rem 1rem;
  }
  .main-wrapper {
    max-width: 500px;
    margin: 0 auto;
  }

  html,
  body {
    margin: 0 auto;
    width: 100%;
    height: 100%;
    color: var(--color-darkest);
  }
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial,
      sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
  }
</style>
